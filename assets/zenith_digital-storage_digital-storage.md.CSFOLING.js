import{_ as l,c as a,o as r,ah as o,j as e,a as n}from"./chunks/framework.PvdZnrZd.js";const u=JSON.parse('{"title":"Digital Storage","description":"Digital Storage","frontmatter":{"title":"Digital Storage","description":"Digital Storage","author":"Chirag Tyagi"},"headers":[],"relativePath":"zenith/digital-storage/digital-storage.md","filePath":"zenith/digital-storage/digital-storage.md"}'),i={name:"zenith/digital-storage/digital-storage.md"};function s(c,t,d,g,m,p){return r(),a("div",null,[...t[0]||(t[0]=[o('<h1 id="user-guide-for-digital-storage" tabindex="-1">User Guide for Digital Storage <a class="header-anchor" href="#user-guide-for-digital-storage" aria-label="Permalink to &quot;User Guide for Digital Storage&quot;">​</a></h1><p>If you are looking to build a marketplace with no restriction of shop type then <a href="/zenith/introduction/">Multi-Vendor Marketplace for Shopify</a> is the solution.</p><p>Selling physical as well as digital products on this Multi-Vendor Marketplace is really flexible &amp; easy.</p><p>In <strong>Multivendor Marketplace App</strong> for Shopify, a merchant can set up a store for physical (normal) products, digital products &amp; even both.</p><p>They can easily select the <strong>Shop Type</strong> from the app&#39;s configuration section. Check out: <a href="/zenith/products/digital-products.html">Sell Digital Products on MultiVendor Marketplace</a></p><p>Now, for adding digital products to your multivendor store, you require digital storage. The app in itself comes with defined storage depending on the subscription plan.</p><p>The digital storage provided in the Multivendor Marketplace for Shopify depends on the plan that you have activated:</p><ul><li>Pro Plan ($60/month)- 15 GB digital storage</li><li>Executive Plan ($40/month)- 5 GB digital storage</li><li>Basic Plan ($15/month)- 3 GB digital storage</li></ul><p>The admin can check this storage by going to <strong>Multi vendor Marketplace Admin Panel</strong> &gt; Hover over the three dots (<strong>⋅⋅⋅</strong>) on the top right of the dashboard &gt; Click on the ‘<strong>Digital Storage</strong>’:</p>',9),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst1.webp",alt:"Digital-Storage-Admin-1",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst1.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),o('<p>In case, if the admin wants to increase this digital storage, then, he can do so by <strong>Upgrading Plan</strong>.</p><h2 id="upgrade-digital-storage-plan" tabindex="-1">Upgrade Digital Storage Plan <a class="header-anchor" href="#upgrade-digital-storage-plan" aria-label="Permalink to &quot;Upgrade Digital Storage Plan&quot;">​</a></h2><p>For this, go to <strong>Multi vendor Marketplace Admin Panel</strong> &gt; Hover over the three dots (<strong>⋅⋅⋅</strong>) on the top right of the dashboard &gt; Click on the ‘<strong>Digital Storage</strong>’ &gt; Click on <strong>Upgrade Plan</strong> button:</p>',3),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst2.webp",alt:"Digital-Storage-Admin-1-1",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst2.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),e("p",null,"This action will take you to the following page:",-1),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst3.webp",alt:"digital storage plan",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst3.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),o('<p>Now here, we have eleven different <strong>Digital Storage Plans</strong> to choose from:</p><ul><li>10 GB for $5/month</li><li>20 GB for $9/month</li><li>30 GB for $13/month</li><li>45 GB for $20/month</li><li>60 GB for $30/month</li><li>120 GB for $55/month</li><li>150 GB for $65/month</li><li>200 GB for $86/month</li><li>225 GB for $100/month</li><li>250 GB for $110/month</li><li>400 GB for $175/month</li></ul><p><strong>NOTE</strong></p><p>Suppose, you are on <strong>PRO</strong> plan of MultiVendor and further you have selected <strong>20 GB digital storage plan</strong> then your total digital storage will become <strong>35 GB</strong></p><p>(Free digital storage that you got with PRO plan: 15 GB <strong>+</strong> additional purchased 20 GB).</p><p>Moreover, we will charge you $9/month for the same, deducting this amount as the Usage Charge.</p><p><strong>Usage Charge</strong>: The variable charges add to the recurring monthly fees of an existing application in use.</p><p>There might be some features in the application, which require an extra amount and that depends on the usage of these features,</p><p>like the number of email count, or the digital storage space you are using.</p><p><strong>NOTE</strong></p><p>Once you select a plan, you CANNOT disable it or opt it out. Also, if you have selected a higher plan, you CANNOT degrade the plan, i.e., move to a lower plan.</p><p>Example: After selecting <strong>20 GB for $9/month plan</strong>, you can&#39;t disable it or move to <strong>10 GB for $5/month plan</strong>. You can only upgrade it to <strong>30 GB for $13/month plan</strong>.</p><h2 id="additional-information" tabindex="-1"><strong>ADDITIONAL INFORMATION</strong> <a class="header-anchor" href="#additional-information" aria-label="Permalink to &quot;**ADDITIONAL INFORMATION**&quot;">​</a></h2><p>If you want to have more than <strong>400 GB</strong> of digital storage on your Multi-Vendor Marketplace, you can simply <strong>contact us</strong>.</p><p>Drop an email at <a href="mailto:support@webkul.com" target="_blank" rel="noreferrer">support@webkul.com</a> regarding the same &amp; we’ll increase your digital storage as per your requirement.</p><p>(This involves extra charges over &amp; above your monthly multivendor plan charges).</p>',16),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst4.webp",alt:"digital storage contact us.",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst4.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),e("h2",{id:"how-to-upgrade-the-digital-storage-plan",tabindex:"-1"},[n("How to upgrade the digital storage plan? "),e("a",{class:"header-anchor",href:"#how-to-upgrade-the-digital-storage-plan","aria-label":'Permalink to "How to upgrade the digital storage plan?"'},"​")],-1),e("p",null,[n("Simply, click on the "),e("strong",null,"Upgrade Storage"),n(" button for the plan that you want to go with. This action will give you the following pop-up:")],-1),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst5.webp",alt:"Upgrade Plan",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst5.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),e("p",null,"You can check the Auto Upgrade Plan option here to automatically upgrade your plan to a higher one once you have used all the current digital storage in your store. (This is completely optional.)",-1),e("p",null,[n("Further, click on the "),e("strong",null,"Upgrade"),n(" button.")],-1),e("p",null,"The admin will receive an email regarding the same.",-1),e("p",null,"This is how the plan status will be visible:",-1),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst6.webp",alt:"Digital Storage Plans",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst6.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),e("p",null,"We will disable the lower plans, and you can switch only to the higher ones as explained above.",-1),e("p",null,[n("Further, you can view the increased digital storage information by going to "),e("strong",null,"Multi vendor Marketplace Admin Panel"),n(" >")],-1),e("p",null,[n("Hover over the three dots ("),e("strong",null,"⋅⋅⋅"),n(") on the top right of the dashboard > Click on the ‘"),e("strong",null,"Digital Storage"),n("’:")],-1),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst1.webp",alt:"Digital-Storage-Admin-2",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst1.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),e("h2",{id:"payment-history",tabindex:"-1"},[n("Payment History "),e("a",{class:"header-anchor",href:"#payment-history","aria-label":'Permalink to "Payment History"'},"​")],-1),e("p",null,[n("You can check the payment history by clicking on the '"),e("strong",null,"Payment History"),n("' button. Refer:")],-1),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst7.webp",alt:"Digital-Storage-Admin-2-1",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst7.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),e("p",null,"The redirected page:",-1),e("p",null,[e("img",{src:"https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst8.webp",alt:"Storage-Payment-History-Admin",style:{cursor:"zoom-in","max-width":"100%"},onclick:`
        const existing = document.getElementById('wk-image-overlay');
        if(existing) existing.remove();

        const overlay = document.createElement('div');
        overlay.id = 'wk-image-overlay';

        overlay.style.cssText = \`
          position:fixed;
          inset:0;
          background:rgba(0,0,0,0.88);
          display:flex;
          align-items:center;
          justify-content:center;
          z-index:99999;
          padding:20px;
          cursor:zoom-out;
          overflow:hidden;
          backdrop-filter: blur(3px);
        \`;

        // 🔹 CLOSE BUTTON
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = '&times;';

        closeBtn.style.cssText = \`
          position:absolute;
          top:20px;
          right:25px;
          color:#fff;
          font-size:42px;
          font-weight:bold;
          cursor:pointer;
          z-index:100000;
          line-height:1;
          user-select:none;
        \`;

        closeBtn.onclick = () => {
          overlay.remove();
        };

        const img = document.createElement('img');
        img.src = "https://cdnblog.webkul.com/blog/wp-content/uploads/2026/05/dgst8.webp";

        img.style.cssText = \`
          max-width:95%;
          max-height:95%;
          border-radius:12px;
          transition:transform .15s ease;
          cursor:zoom-in;
          will-change:transform;
          user-select:none;
        \`;

        let scale = 1;

        // scroll zoom
        overlay.onwheel = (e) => {
          e.preventDefault();

          scale += e.deltaY * -0.001;
          scale = Math.min(Math.max(1, scale), 5);

          img.style.transform = 'scale(' + scale + ')';
        };

        // double click zoom
        img.ondblclick = () => {
          scale = scale === 1 ? 2 : 1;
          img.style.transform = 'scale(' + scale + ')';
        };

        overlay.appendChild(closeBtn);
        overlay.appendChild(img);

        document.body.appendChild(overlay);

        // close overlay
        overlay.onclick = (e) => {
          if (e.target === overlay) {
            overlay.remove();
          }
        };

        // esc close
        document.onkeydown = (e) => {
          if (e.key === 'Escape') {
            overlay.remove();
          }
        };
      `})],-1),o('<h3 id="digital-storage-is-used-to-store-the-following-files" tabindex="-1">Digital storage is used to store the following files: <a class="header-anchor" href="#digital-storage-is-used-to-store-the-following-files" aria-label="Permalink to &quot;Digital storage is used to store the following files:&quot;">​</a></h3><ul><li><strong>Product Image</strong></li><li><strong>Store Logo</strong></li><li><strong>Shop Logo</strong></li><li><strong>Store Banner</strong></li><li><strong>Checkout Image</strong></li><li><strong>Shipping Label</strong></li><li><strong>Seller Shop Logo</strong></li><li><strong>Icon Image</strong></li><li><strong>Favicon Image</strong></li><li><strong>Badge Image</strong>: Used to store seller badges in case you are using the <a href="/zenith/featured-app/seller-add-ons/seller-and-customer-badge.html">Seller Badge</a> feature app.</li></ul><h6 id="some-more" tabindex="-1">Some More… <a class="header-anchor" href="#some-more" aria-label="Permalink to &quot;Some More…&quot;">​</a></h6><ul><li><strong>Image CSV</strong>: This will store the images upload via CSV on your marketplace.</li><li><strong>Image Zip</strong>: This will store the images uploaded via a zip file on your marketplace.</li><li><strong>Seller CSV</strong>: This will store the CSV file used to upload sellers on your marketplace.</li><li><strong>Product CSV</strong>: This will store the CSV file used to upload products on your marketplace.</li><li><strong>Product Properties</strong>: Any file you use in your product’s properties will be stored here.</li><li><strong>Custom Fields</strong>: The files used in the created custom fields will be stored here.</li><li><strong>Product Design</strong>: The initial replacement value proof will be stored here. (Only for merchants using the ‘<a href="/zenith/featured-app/marketplace-product-add-ons/artist-product-design.html">Artist Product Design</a>‘ feature app.)</li><li><strong>Digital Order File</strong>: This will store the order files for the <a href="/zenith/products/digital-products.html">digital products</a> in your store.</li><li><strong>Digital Order File Service</strong>: The order files in case of <a href="/zenith/products/digital-products.html#digital-product-as-service">digital products as service</a>.</li><li><strong>Logo Image</strong>: This will store the logo of the merchant.</li><li><strong>Fulfillment</strong>: This will store the other files related to order fulfillment.</li><li><strong>Manifest</strong>: This will store the manifest details. (Only for merchants using ‘<a href="/zenith/featured-app/seller-add-ons/pwa.html">PWA for Sellers</a>‘ feature app.)</li><li><strong>Manifest Icon</strong>: This will store the image used in the manifest configuration. (Only for merchants using ‘<a href="/zenith/featured-app/seller-add-ons/pwa.html">PWA for Sellers</a>‘ feature app.)</li><li><strong>PWA Icon</strong>: This will store the PWA icon used in your marketplace. (Only for merchants using ‘<a href="/zenith/featured-app/seller-add-ons/pwa.html">PWA for Sellers</a>‘ feature app.)</li><li><strong>OG Image</strong>: Will store the OG image used in ‘<a href="/zenith/featured-app/seller-add-ons/pwa.html">PWA for sellers</a>‘ &amp; ‘<a href="/zenith/featured-app/connector/seller-shopify-store-connector.html">Create an app for sellers</a>‘ feature app.</li></ul><h4 id="💡-need-help" tabindex="-1">💡 Need Help? <a class="header-anchor" href="#💡-need-help" aria-label="Permalink to &quot;💡 Need Help?&quot;">​</a></h4><p>Drop your query/suggestion at <a href="mailto:support@webkul.com" target="_blank" rel="noreferrer">support@webkul.com</a> or create your ticket at <a href="https://webkul.uvdesk.com/en/customer/create-ticket/" target="_blank" rel="noreferrer">Webkul UV Desk</a></p><h3 id="schedule-demo" tabindex="-1">SCHEDULE DEMO <a class="header-anchor" href="#schedule-demo" aria-label="Permalink to &quot;SCHEDULE DEMO&quot;">​</a></h3><p><a href="https://egsma.io/shopify-multivendor-marketplace/" target="_blank" rel="noreferrer">Click here to Schedule the demo of Multivendor marketplace App for Shopify </a></p>',8)])])}const y=l(i,[["render",s]]);export{u as __pageData,y as default};
